from math import *

n = input('Введите значение для переменной x: ')

y = 0


if n.isdigit() == False:
    print('Введенное вами значение не является числом')

else:
    n = int(n)
    if n <= pi / 4:
        y = sin(n)

    if n > pi / 4:
        y = cos(n)

    print(y)

